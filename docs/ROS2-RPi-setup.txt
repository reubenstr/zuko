General instructions on installing ROS2 on the Raspberry Pi
Work in progress, not ready for replication

Install Ubuntu 20.04 and setup Wi-Fi:
	https://roboticsbackend.com/install-ubuntu-on-raspberry-pi-without-monitor/
	
Install ROS2 Foxy on Raspberry Pi 4:

	https://roboticsbackend.com/install-ros2-on-raspberry-pi/
	
Clone repository and build:
	git clone https://github.com/reubenstr/rpi_quad_ws
	cd rpi_quad_ws
	colcon build
	
source ws:
		source install/local_setup.bash
	

Grant access to I2C:
	/* DID NOT WORK
	sudo apt install rpi.gpio-common
	sudo adduser "${USER}" input
	sudo reboot
	*/
	
	sudo chmod a+rw /dev/i2c-*


Generate servo config yaml file
	gen_servo... TODO

	
########	
	
I2C
	https://cdn-learn.adafruit.com/downloads/pdf/adafruit-16-channel-servo-driver-with-raspberry-pi.pdf	
	
	sudo apt-get install i2c-tools
	
	verify i2c device is detected:
		sudo i2cdetect -y 1
	
	
	sudo pip3 install adafruit-circuitpython-servokit
	
########


calibrate servos, run tool to generate calibration file prior to launching quad
	###colcon build --packages-select quad; ros2 launch quad motion_servo_calibration.launch.py 	
	
	
For micro-ros:

	micro-ros likey needs to be rebuild based on the architecture 
	change from AMD to ARM.

	---
	sudo apt install python3-rosdep2
	sudo apt update && rosdep update
	rosdep install --from-path src --ignore-src -y

	colcon build
	source install/local_setup.bash
	
	---
	ros2 run micro_ros_setup create_agent_ws.sh
	ros2 run micro_ros_setup build_agent.sh
	source install/local_setup.bash
	---
	ros2 run micro_ros_agent micro_ros_agent serial --dev /dev/ttyACM0
	
	https://micro.ros.org/docs/tutorials/core/first_application_linux/
	
	